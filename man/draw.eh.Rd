% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DrawSyntenyPlot.R, R/draw_eh_adjS.R
\name{draw.eh}
\alias{draw.eh}
\title{Evolution Highway style plot}
\usage{
draw.eh(
  output,
  chrRange,
  data_file,
  directory = NULL,
  fileformat = "png",
  colour = "lightblue",
  inverted.colour = "lightpink",
  w = 5.5,
  h = 10,
  ps = 10,
  adj_file = NULL,
  adj_point_size = 1.1,
  adj_line = TRUE,
  adj_colour_low = "#FDB863",
  adj_colour_high = "#B2182B",
  adj_panel_frac = 0.02,
  adj_score_transform = c("identity", "sqrt", "log10"),
  strip_angle = 90,
  strip_text_size = 10,
  shorten_block_labels = TRUE,
  shorten_n = 3,
  label_fixed_size = 4,
  label_scale_by_width = TRUE,
  label_width_exponent = 0.5,
  label_min = 2,
  label_max = 10
)

draw.eh(
  output,
  chrRange,
  data_file,
  directory = NULL,
  fileformat = "png",
  colour = "lightblue",
  inverted.colour = "lightpink",
  w = 5.5,
  h = 10,
  ps = 10,
  adj_file = NULL,
  adj_point_size = 1.1,
  adj_line = TRUE,
  adj_colour_low = "#FDB863",
  adj_colour_high = "#B2182B",
  adj_panel_frac = 0.02,
  adj_score_transform = c("identity", "sqrt", "log10"),
  strip_angle = 90,
  strip_text_size = 10,
  shorten_block_labels = TRUE,
  shorten_n = 3,
  label_fixed_size = 4,
  label_scale_by_width = TRUE,
  label_width_exponent = 0.5,
  label_min = 2,
  label_max = 10
)
}
\arguments{
\item{output}{Character string. Prefix for output file names.}

\item{chrRange}{Character vector. Chromosome(s) to plot.}

\item{data_file}{Character string. Path to EH alignment file.}

\item{directory}{Character string. Output directory. Defaults to a temporary directory.}

\item{fileformat}{Character string. Output image format (e.g. "png", "pdf").}

\item{colour}{Character string. Fill colour for forward-oriented blocks.}

\item{inverted.colour}{Character string. Fill colour for inverted blocks.}

\item{w}{Numeric. Width of the output image.}

\item{h}{Numeric. Height of the output image.}

\item{ps}{Numeric. Point size used when saving the plot.}

\item{adj_file}{Character string or NULL. Optional file with adjacency scores
(e.g. converted from DESCHRAMBLER). If NULL, no adjacency panel is drawn.}

\item{adj_point_size}{Numeric. Size of points in the adjacency score panel.}

\item{adj_line}{Logical. Whether to draw a connecting line between adjacency score points.}

\item{adj_colour_low}{Character string. Low colour for adjacency score gradient.}

\item{adj_colour_high}{Character string. High colour for adjacency score gradient.}

\item{adj_panel_frac}{Numeric. Fraction of total plot width allocated to the
adjacency score panel (between 0 and 1).}

\item{adj_score_transform}{Character. Optional transformation applied to
adjacency scores ("identity", "sqrt", or "log10").}

\item{strip_angle}{Numeric. Angle of species (facet) labels.}

\item{strip_text_size}{Numeric. Text size of species (facet) labels.}

\item{shorten_block_labels}{Logical. Whether to shorten chromosome labels
inside EH blocks.}

\item{shorten_n}{Integer. Number of characters kept from the right when
shortening block labels.}

\item{label_fixed_size}{Numeric. Base font size for labels inside EH blocks.}

\item{label_scale_by_width}{Logical. Whether to scale block label size by the
number of species panels.}

\item{label_width_exponent}{Numeric. Exponent controlling how strongly label
size decreases with increasing number of species panels.}

\item{label_min}{Numeric. Minimum allowed block label font size.}

\item{label_max}{Numeric. Maximum allowed block label font size.}
}
\value{
Invisibly returns NULL. The function is called for its side effect
of saving one image per chromosome.

Invisibly returns NULL. The function is called for its side effect
of saving one image per chromosome.
}
\description{
Draw Evolution Highway Plots

This function plots EH blocks for one or more chromosomes and, optionally,
a side panel showing DESCHRAMBLER adjacency scores aligned to genomic
coordinates. It extends the original \code{draw.eh} by allowing control over
label shortening, label size, and adjacency panel width.
}
\details{
This function draws Evolution Highway style plots.

It requires as input:
\enumerate{
\item Desired output file name
\item The range of chromosomes of the reference species this is entered as either a single number i.e. 1 or a range of numbers i.e. 1:22.
\emph{Note: if you are inputting sex chromosomes or chromosomes with characters in the name input a concatenated string i.e. c(1,2,3,"X")}
\item File containing the syntenic blocks of alignments of one or more target species aligned to a single reference; following this format:
reference chromosome, reference start position, reference end position, target chromosome,
target start position, target end position, orient, reference species identifier, target species identifier
}

There are optional parameters for some customization of this function:
\enumerate{
\item The format for saving the image i.e. png or pdf can be altered by inputting: \code{fileformat = "pdf"} (the default value is "png")
\item The colour of the syntenic blocks (not inverted blocks) can be changed by inputting: \code{colour = "red"} (the default value is "lightblue", see Rcolour pallette for colour options)
\item The colour of the inverted syntenic blocks can be changed by inputting: \code{inverted.colour = "blue"} (the default value is "lightpink", see Rcolour pallette for colour options)
\item The width of the image created can be changed by inputting: \code{w = 5.5}
\item The height of the image created can be changed by inputting: \code{h = 10}
\item The point size of the image created can be changed by inputting: \code{ps = 10}
\item The directory where the image file should be saved, as default the image is saved to temporary directory, change by inputting: \code{directory = "path/to/directory"}
}

The function works creating a graph for each reference chromosome using their start and end positions to create a block for the reference
and the target chromosome positions are used to colour the region where synteny was identified

Example: \code{draw.eh("outputName",c(17,"X"), "example_eh_alignments_2.txt", directory = "path/to/directory", fileformat = "pdf")}

Adjacency scores are expected to be numeric values between 0 and 1 and are
plotted as a heatmap (orange to red) along genomic coordinates. One-to-many
mappings are retained in the adjacency panel.

Adjacency scores are expected to be numeric values between 0 and 1 and are
plotted as a heatmap (orange to red) along genomic coordinates. One-to-many
mappings are retained in the adjacency panel.
}
\seealso{
\code{\link{draw.linear}}

\code{\link{draw.linear}}
}
